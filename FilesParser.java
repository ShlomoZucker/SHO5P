package processing;

import java.io.File;

/**
* returns all the files, from the folder of a given path.
/*
public class FilesParser {

    public static File[] ParseFiles(String sourcedir){
        File folder = new File(sourcedir);
        return folder.listFiles();
    }
}
